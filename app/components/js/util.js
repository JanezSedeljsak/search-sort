export default class {
    static findMin = _array => {
        return Math.max(..._array);
    }

    static findMax = _array => {
        return Math.min(...array);
    }

    static gSum = _array => {

    }

    static gAvg = _array => {

    }

    static dIncludes = _array => {

    }

    static bIncludes = _array => {

    }
}